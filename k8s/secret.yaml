apiVersion: v1
kind: Secret
metadata:
  name: fraud-detection-secrets
  namespace: fraud-detection
  labels:
    app.kubernetes.io/name: fraud-detection
    app.kubernetes.io/component: secret
type: Opaque
data:
  # 🤖 MLflow Configuration (base64 encoded)
  # echo -n "http://your-mlflow-server:5000" | base64
  MLFLOW_TRACKING_URI: aHR0cDovL3lvdXItbWxmbG93LXNlcnZlcjo1MDAw
  
  # echo -n "your-mlflow-username" | base64
  MLFLOW_TRACKING_USERNAME: ""
  
  # echo -n "your-mlflow-password" | base64  
  MLFLOW_TRACKING_PASSWORD: ""
  
  # 🔐 API Security (optional)
  # echo -n "your-api-key" | base64
  API_KEY: ""
  
  # 🗄️ Database URL (optional)
  # echo -n "postgresql://user:pass@host:5432/db" | base64
  DATABASE_URL: ""

---
# Примеры для замены значений:
# kubectl create secret generic fraud-detection-secrets \
#   --from-literal=MLFLOW_TRACKING_URI="http://mlflow.default.svc.cluster.local:5000" \
#   --from-literal=MLFLOW_TRACKING_USERNAME="" \
#   --from-literal=MLFLOW_TRACKING_PASSWORD="" \
#   --from-literal=API_KEY="your-secure-api-key" \
#   --namespace=fraud-detection
